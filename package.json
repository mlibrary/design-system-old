{
  "name": "umich-lib-design-system",
  "private": true,
  "devDependencies": {
    "@types/node": "^12.12.11",
    "lerna": "^3.19.0"
  },
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "start:packages": "lerna run start --stream --parallel",
    "start": "npm run bootstrap && npm run start:packages"
  }
}
