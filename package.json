{
  "name": "umich-lib-design-system",
  "private": true,
  "devDependencies": {
    "@types/node": "^12.12.11",
    "lerna": "^3.19.0"
  },
  "scripts": {
    "lerna:bootstrap": "lerna bootstrap",
    "packages:start": "lerna run start --stream --parallel",
    "packages:build": "lerna run build --stream",
    "start": "npm run lerna:bootstrap && npm run packages:start",
    "build": "npm run lerna:bootstrap && npm run packages:build"
  }
}
