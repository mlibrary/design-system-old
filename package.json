{
  "name": "umich-lib-design-system",
  "private": true,
  "devDependencies": {
    "lerna": "^3.20.2"
  },
  "scripts": {
    "lerna:bootstrap": "lerna bootstrap --hoist",
    "lerna:publish": "lerna publish",
    "prelerna:publish-force": "npm run build",
    "lerna:publish-force": "lerna publish --force-publish",
    "build": "lerna run build --stream --parallel --scope @umich-lib/tokens --scope @umich-lib/css --scope @umich-lib/components --scope @umich-lib/icons && lerna run build --ignore @umich-lib/css --ignore @umich-lib/components --ignore @umich-lib/icons",
    "start": "lerna run start --stream --parallel"
  }
}
