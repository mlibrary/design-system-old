{
  "name": "umich-lib-design-system",
  "private": true,
  "devDependencies": {
    "@types/node": "^12.12.11",
    "lerna": "^3.19.0"
  },
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "start": "npm run bootstrap && lerna run start --stream --parallel"
  }
}
