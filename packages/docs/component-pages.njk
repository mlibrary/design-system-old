---
layout: layout.njk
pagination:
    data: components
    alias: component
    size: 1
permalink: "components/{{ component.name | slug }}/"
templateEngineOverride: njk,md
eleventyComputed:
  title: "{{ component.name }}"
  isExperimental: {{ component.isExperimental }}
  eleventyNavigation:
    key: "{{ component.name }} | Components"
    parent: Components
  description: "{{ component.description }}"
---

{% if component.isExperimental %}
  <span class="badge badge--warning">Experimental</span>
{% endif %}

{{ component.description }}

## Example

<div class="example">
  <div class="example__preview">
    {{ component.example | safe }}
  </div>

  <div class="m-4">
    <form action="https://codepen.io/pen/define" method="POST" target="_blank">
      <input
    type="hidden"
    name="data"
    value='{
      "title": "{{ component.name }} — Design System — University of Michigan Library",
      "html": "{{ site.codepen_html_comment | safe | replace('"', '\\"') | replace('\'', '&#39;') | replace('\n', '\\n')}} {{ component.example | safe | replace('"', '\\"') | replace('\'', '&#39;') | replace('\n', '\\n') }}",
      "css_external": "https://unpkg.com/@umich-lib/css@v1/dist/umich-lib.css",
      "js_external": "https://unpkg.com/@umich-lib/components@v1/dist/umich-lib/umich-lib.js",
      "layout": "left",
      "editors": "100"
    }'
/>
    <input type="submit" value="Edit in CodePen" 
    class="bg-blue-100 text-teal-400 px-4 py-3 rounded cursor-pointer font-bold hover:shadow-outline text-base"/>
  </form>
</div>

```html
{{ component.example | safe }}
  ```

  </div>
{% if component.props %}
## Properties

| Attribute | Type | Default | Description |
| --- | ---| --- | --- |
{% for prop in component.props %} | `{{ prop.attr }}` | `{{ prop.type }}` | {{ prop.default }} |  {{ prop.docs }} |
{% endfor %}
{% endif %}

{% if component.whenToUse %}
## When to use this component
{{ component.whenToUse }}
{% endif %}

{% if component.whenNotToUse %}
## When not to use this component
{{ component.whenNotToUse }}
{% endif %}

{% if component.usedBy %}
## Used by
{{ component.usedBy }}
{% endif %}

{% if component.research %}
## Research
{{ component.research }}
{% endif %}

## Integration
For integration, please see [getting started](/#getting-started) documentation.